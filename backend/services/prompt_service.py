class PromptService:
    """Still in development, this service is responsible for building prompts based on the provided payload."""
    def __init__(self):
        pass

    def build(self, payload):
        """
        Build the prompt based on the payload provided.

        Args:
            payload (dict): A dictionary containing the necessary information to build the prompt.

        Returns:
            str: The constructed prompt string.
        """

        # TODO: Implement full prompt building logic based on payload
        raise NotImplementedError("PromptService.build() is still under development.")